[package]
name = "inventory-service"
edition = "2021"
version = "0.1.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.29", features = ["default"] }
common = { path = "../common" }
serde_json = "1.0.117"
serde = { version = "1.0", features = ["derive"] }
